// Access Check:
// Check if a user is both logged in and verified before showing a secret message.

// const isLogIn = true;
// const isVerified = false;

// if (isLogIn && isVerified) {
//   alert("This is a top secrect mission");
// } else {
//   alert("You're not allowed here");
// }

// Age and Membership:
// Determine if a user is at least 18 years old and is a registered member before allowing entry to a restricted section.

// const age = 29;
// const registeredMember = true;

// if (age && registeredMember) {
//   console.log("You can enter Sumi ");
// } else {
//   console.log("You are not allowed to enter");
// }

// Number Range:
// Verify if a number is between 10 and 50 (inclusive) using the && operator.

// const number1 = Math.random() * 2 + 5;
// const number2 = Math.random() * 5;
// const sumOfTwo = number1 * number2;

// const result = sumOfTwo.toFixed(0);

// Login Validation:
// Validate a login by checking if both the username and password are correct.

Button Interaction:
Display a message only if two buttons have been clicked (both variables are true).
